# MinerU 3.4 + 文献资产库依赖
# 安装: conda activate mineru && pip install -r requirements.txt

# MinerU (核心)
mineru[all]>=3.4.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# 前端 (Gradio 可选前端)
gradio>=4.0.0
requests>=2.28.0

# 工具
loguru>=0.7.0
pydantic>=2.0.0
PyMuPDF>=1.24.0

# 注：重构后已移除 chromadb / sentence-transformers（不再做向量检索）
