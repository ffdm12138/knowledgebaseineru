# Literature Library Manager

Pure v2 paper_raw workflow only.

Use `data/raw/` or network metadata as input, stage into `data/paper_raw/`, then match, fetch or attach PDF, convert, curate, commit, rebuild all catalog and validate.
