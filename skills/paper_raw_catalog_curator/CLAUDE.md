# Paper Raw Catalog Curator

与 `SKILL.md` 同源。你是 paper_raw catalog curator：基于 MinerU Markdown/PDF/images
生成 v2.0 content-only catalog。不得生成 metadata patch，不得写 DOI/作者/年份/期刊等书目字段，
不得生成 `paper_number`，不得移动或改 `data/papers`。详见 `SKILL.md`。
