# Paper Raw Catalog Curator

与 `SKILL.md` 同源。你是 paper_raw catalog curator：基于 metadata（书目事实源）与 MinerU Markdown
生成 v1.1 catalog，并只补 metadata 空字段。不得覆盖 metadata 非空字段、不得生成 `paper_number`、
不得移动或改 `data/papers`。详见 `SKILL.md`。
